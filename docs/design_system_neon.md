Design System: HUD Neon (初版)
目标：统一颜色、字体、间距、发光等风格，便于后续快速迭代与主题切换。

核心要点
- 颜色令牌：背景、前景、边框、发光等
- 字体令牌：字体族、字号
- 发光工具：glow(widget, color, radius)
- 应用入口：apply_neon_theme(widget)

后续扩展
- 扩展 tokens、增加 DesignPreview 面板、支持多主题切换。
